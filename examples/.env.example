# ADK MCP Toolkit Examples - Environment Configuration

# Required: Google API Key for Gemini (agent LLM)
GOOGLE_API_KEY=your-google-api-key

# Required: Google Cloud Project ID (for Vertex AI APIs)
PROJECT_ID=your-gcp-project-id

# Optional: GCP Region (default: us-central1)
LOCATION=us-central1

# Optional: GCS Bucket for cloud storage output
# Required for video generation (Veo API requires GCS output)
GCS_BUCKET=your-gcs-bucket

# Optional: Logging level
RUST_LOG=info
